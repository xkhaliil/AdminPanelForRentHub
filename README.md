# AdminPanelForRentHub

Android admin panel for the RentHub rental application.

![Java](https://img.shields.io/badge/language-Java-orange)

## What it does

<!-- TODO: describe the app's screens and admin features once source files are available -->
This repository contains the Android client for an admin panel tied to the "RentHub" application. The project is structured as a standard Gradle-based Android app (`app/` module with `androidTest` and `test` source sets) and includes a `google-services.json` file, indicating it integrates with Firebase. Beyond this structural setup, the specific admin features and screens could not be determined from the available evidence.

## Tech stack

- Java
- Android (Gradle Kotlin DSL build scripts: `build.gradle.kts`, `settings.gradle.kts`)
- Firebase (via `app/google-services.json`)

## Getting started

This is an Android Studio / Gradle project. Open it in Android Studio, or build from the command line:

```
./gradlew build
```

<!-- TODO: confirm minimum SDK, required local.properties/API keys, and any Firebase setup steps needed to run the app -->

## License

No license file is present in this repository yet.
